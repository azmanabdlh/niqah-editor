import Editor from './editor';

/*
 * import Editor from '@/niqah-editor';
 *
 * const editor = new Editor({ canvasId: 'homepage', title: 'Homepage' })
 *   .init(() => {})
 *   .onSave(async (schema) => {
 *    
 *   })
 *   .onPublish(async (schema) => {
 *    
 *   })
 *   .mount('#container');
 */


export {
  Editor,
}